---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION, ORG_INFO } from '../consts';
---

<!doctype html>
<html lang="pl">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<img src="/stowarzyszenie-logo.png" alt="Logo Stowarzyszenia Nadolice Wielkie" class="hero-logo" />
				<h1>Witamy w Stowarzyszeniu Nadolice Wielkie</h1>
				<p class="subtitle">Działamy na rzecz rozwoju wsi Nadolice Wielkie</p>
			</section>

			<section class="about-section">
				<h2>O Stowarzyszeniu</h2>
				<p>
					Stowarzyszenie Nadolice Wielkie to organizacja społeczna działająca od 2015 roku 
					na rzecz rozwoju lokalnej społeczności. Naszym celem jest integracja mieszkańców 
					oraz wspieranie inicjatyw służących poprawie jakości życia w naszej wsi.
				</p>
			</section>

			<section class="info-cards">
				<div class="card">
					<h3>Dane rejestrowe</h3>
					<ul>
						<li><strong>KRS:</strong> {ORG_INFO.krs}</li>
						<li><strong>NIP:</strong> {ORG_INFO.nip}</li>
						<li><strong>REGON:</strong> {ORG_INFO.regon}</li>
						<li><strong>Data rejestracji:</strong> {ORG_INFO.registrationDate}</li>
					</ul>
				</div>

				<div class="card">
					<h3>Siedziba</h3>
					<p>{ORG_INFO.address}</p>
				</div>

				<div class="card">
					<h3>Dokumenty</h3>
					<p>
						<a href="#" class="download-link">Pobierz statut stowarzyszenia (wkrótce)</a>
					</p>
				</div>
			</section>

			<section class="cta-section">
				<h2>Dołącz do nas</h2>
				<p>
					Zapraszamy do współpracy i udziału w naszych inicjatywach. 
					Śledź nasze aktualności na Facebooku!
				</p>
				<a href={ORG_INFO.socialMedia.facebook} target="_blank" class="cta-button">
					Odwiedź nas na Facebooku
				</a>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.hero {
		text-align: center;
		padding: 3em 1em;
		background: linear-gradient(135deg, #18489F 0%, #59CBEF 100%);
		color: white;
		margin-bottom: 2em;
		border-radius: 12px;
	}
	.hero-logo {
		max-width: 150px;
		height: auto;
		margin: 0 auto 1.5em;
		display: block;
	}
	.hero h1 {
		margin: 0 0 0.5em 0;
		font-size: 2.5em;
		line-height: 1.2;
	}
	.subtitle {
		font-size: 1.3em;
		margin: 0;
		opacity: 0.95;
	}
	.about-section {
		max-width: 800px;
		margin: 0 auto 3em;
		padding: 0 1em;
	}
	.about-section h2 {
		color: #18489F;
		margin-bottom: 1em;
	}
	.about-section p {
		line-height: 1.6;
		font-size: 1.1em;
	}
	.info-cards {
		display: grid;
		grid-template-columns: 1fr;
		gap: 2em;
		max-width: 800px;
		margin: 0 auto 3em;
		padding: 0 1em;
	}
	.card {
		background: white;
		padding: 1.5em;
		border-radius: 8px;
		box-shadow: 0 2px 8px rgba(var(--black), 10%);
	}
	.card h3 {
		margin-top: 0;
		color: #18489F;
	}
	.card ul {
		list-style: none;
		padding: 0;
	}
	.card ul li {
		padding: 0.5em 0;
		border-bottom: 1px solid rgba(var(--gray), 0.2);
	}
	.card ul li:last-child {
		border-bottom: none;
	}
	.download-link {
		color: #18489F;
		text-decoration: none;
		font-weight: 500;
	}
	.download-link:hover {
		text-decoration: underline;
	}
	.cta-section {
		text-align: center;
		padding: 3em 1em;
		background: rgba(36, 183, 131, 0.05);
		margin-top: 2em;
	}
	.cta-section h2 {
		color: #18489F;
		margin-bottom: 0.5em;
	}
	.cta-section p {
		font-size: 1.1em;
		margin-bottom: 1.5em;
	}
	.cta-button {
		display: inline-block;
		padding: 0.8em 2em;
		background: #18489F;
		color: white;
		text-decoration: none;
		border-radius: 5px;
		font-weight: 600;
		transition: transform 0.2s, box-shadow 0.2s;
	}
	.cta-button:hover {
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(24, 72, 159, 0.3);
		background: #24B783;
	}
	@media (max-width: 720px) {
		.hero h1 {
			font-size: 1.8em;
		}
		.subtitle {
			font-size: 1.1em;
		}
	}
</style>
