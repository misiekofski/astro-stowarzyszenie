---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, ORG_INFO } from '../consts';
---

<!doctype html>
<html lang="pl">
	<head>
		<BaseHead title={`O nas - ${SITE_TITLE}`} description="Informacje o Stowarzyszeniu Nadolice Wielkie i zarządzie" />
	</head>
	<body>
		<Header />
		<main>
			<section class="page-header">
				<h1>O Stowarzyszeniu</h1>
			</section>

			<section class="content-section">
				<div class="about-content">
					<h2>Nasza misja</h2>
					<p>
						Stowarzyszenie Nadolice Wielkie zostało założone w 2015 roku z myślą o rozwoju 
						lokalnej społeczności i poprawie jakości życia mieszkańców naszej wsi. 
						Działamy na rzecz integracji mieszkańców, wspieramy lokalne inicjatywy 
						oraz dbamy o zachowanie tradycji i kultury naszego regionu.
					</p>

					<h2>Zarząd Stowarzyszenia</h2>
					<div class="board-members">
						<div class="member-card">
							<div class="member-role">Prezes</div>
							<div class="member-name">{ORG_INFO.board.president}</div>
						</div>

						<div class="member-card">
							<div class="member-role">Wiceprezes</div>
							<div class="member-name">{ORG_INFO.board.vicePresident}</div>
						</div>

						<div class="member-card">
							<div class="member-role">Skarbnik</div>
							<div class="member-name">{ORG_INFO.board.treasurer}</div>
						</div>

						<div class="member-card">
							<div class="member-role">Członek Zarządu</div>
							<div class="member-name">{ORG_INFO.board.boardMember}</div>
						</div>
					</div>

					<h2>Dane organizacji</h2>
					<div class="org-details">
						<div class="detail-row">
							<span class="detail-label">Pełna nazwa:</span>
							<span class="detail-value">{ORG_INFO.fullName}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Forma prawna:</span>
							<span class="detail-value">{ORG_INFO.legalForm}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Data rejestracji:</span>
							<span class="detail-value">{ORG_INFO.registrationDate}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">KRS:</span>
							<span class="detail-value"><a href="https://rejestr.io/krs/540080/stowarzyszenie-nadolice-wielkie" target="_blank">
							{ORG_INFO.krs}</a></span>
						</div>
						<div class="detail-row">
							<span class="detail-label">NIP:</span>
							<span class="detail-value">{ORG_INFO.nip}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">REGON:</span>
							<span class="detail-value">{ORG_INFO.regon}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Adres siedziby:</span>
							<span class="detail-value">{ORG_INFO.address}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Numer konta:</span>
							<span class="detail-value">{ORG_INFO.bankAccount}</span>
						</div>
					</div>

					<h2>Dokumenty</h2>
					<div class="documents">
						<a href="#" class="document-link">
							<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
								<polyline points="14 2 14 8 20 8"></polyline>
								<line x1="12" y1="18" x2="12" y2="12"></line>
								<line x1="9" y1="15" x2="15" y2="15"></line>
							</svg>
							<span>Statut Stowarzyszenia (wkrótce dostępny)</span>
						</a>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.page-header {
		text-align: center;
		padding: 2em 1em;
		background: linear-gradient(135deg, #18489F 0%, #59CBEF 100%);
		color: white;
	}
	.page-header h1 {
		margin: 0;
		font-size: 2.5em;
	}
	.content-section {
		max-width: 900px;
		margin: 0 auto;
		padding: 3em 1em;
	}
	.about-content h2 {
		color: #18489F;
		margin-top: 2em;
		margin-bottom: 1em;
	}
	.about-content h2:first-of-type {
		margin-top: 0;
	}
	.about-content p {
		line-height: 1.6;
		font-size: 1.1em;
		margin-bottom: 1.5em;
	}
	.board-members {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 1.5em;
		margin: 2em 0;
	}
	.member-card {
		background: white;
		padding: 1.5em;
		border-radius: 8px;
		box-shadow: 0 2px 8px rgba(var(--black), 10%);
		text-align: center;
		border-top: 3px solid #24B783;
	}
	.member-role {
		font-size: 0.9em;
		color: rgb(var(--gray));
		text-transform: uppercase;
		letter-spacing: 0.05em;
		margin-bottom: 0.5em;
	}
	.member-name {
		font-size: 1.1em;
		font-weight: 600;
		color: rgb(var(--black));
	}
	.org-details {
		background: white;
		padding: 1.5em;
		border-radius: 8px;
		box-shadow: 0 2px 8px rgba(var(--black), 10%);
	}
	.detail-row {
		display: flex;
		padding: 0.8em 0;
		border-bottom: 1px solid rgba(var(--gray), 0.2);
	}
	.detail-row:last-child {
		border-bottom: none;
	}
	.detail-label {
		font-weight: 600;
		min-width: 200px;
		color: rgb(var(--gray-dark));
	}
	.detail-value {
		flex: 1;
		color: rgb(var(--black));
	}
	.documents {
		margin-top: 1em;
	}
	.document-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5em;
		padding: 1em 1.5em;
		background: white;
		border: 2px solid #18489F;
		border-radius: 8px;
		color: #18489F;
		text-decoration: none;
		font-weight: 500;
		transition: all 0.2s;
	}
	.document-link:hover {
		background: #18489F;
		color: white;
	}
	.document-link svg {
		flex-shrink: 0;
	}
	@media (max-width: 720px) {
		.page-header h1 {
			font-size: 1.8em;
		}
		.detail-row {
			flex-direction: column;
			gap: 0.3em;
		}
		.detail-label {
			min-width: auto;
		}
	}
</style>
